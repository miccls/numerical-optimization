import numpy as np

type ArrayF = np.typing.NDArray[
    np.floating
]  # Arbitrary shape, generic numerical precision

type ArrayI = np.typing.NDArray[
    np.integer
]  # Arbitrary shape, generic numerical precision
